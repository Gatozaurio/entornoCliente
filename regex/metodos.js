const regex1 = /^([6-7]{1}[0-9]{8})$/;
const regex1 = /^[a-zA-Z0-9_]{5,} @[a-z]{5, }\.es$/;

function comprobarNumero(numero){
    if(numero.match(regex)){

    }
}

function comprobarNumero(email){
    if(email.match(regex2)){
            
    }
}

function contarPalabra(frase, palabra){
    match(palabra)
}

contarPalabra("Estoy cansado de esta clase de cliente, denada", "de");